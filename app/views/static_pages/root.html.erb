<script type="text/javascript">
  <% if logged_in? %>
    window.currentUser = <%= render(
      "api/users/user.json.jbuilder",
      user: current_user
    ).html_safe %>
  <% end %>

  window.images = {
    beatWaveLogo: "<%= image_url("beat-wave-logo.png") %>",
    splashHeader: "<%= image_url("crate-digging") %>"
}
</script>

<main id='root'></main>
